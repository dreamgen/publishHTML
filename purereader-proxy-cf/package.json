{
  "name": "purereader-proxy-cf",
  "version": "1.1.0",
  "private": true,
  "description": "PureReader CORS proxy + htmlShare 上傳／分享後端，共用同一個 Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
